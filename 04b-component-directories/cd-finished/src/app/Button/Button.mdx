import { Canvas, Meta } from "@storybook/blocks";

import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Button

You should use the `Button` component when you need a button.

<Canvas of={ButtonStories.Primary} />
